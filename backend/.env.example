# Only needed if you run the backend OUTSIDE Docker (e.g. pytest in your editor).
# Copy to backend/.env. Inside Docker, values come from deploy/docker-compose.dev.yml.
ENVIRONMENT=development
LOG_FORMAT=console
DATABASE_URL=postgresql+psycopg://app:app@localhost:5432/app
REDIS_URL=redis://localhost:6379/0
