# Runtime dependencies — exact pins. Update deliberately, then run the tests.
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
pydantic-settings==2.15.0
sqlalchemy==2.0.54
alembic==1.20.0
psycopg[binary]==3.3.6
redis==6.4.0  # kombu (Celery) requires redis<6.5
celery[redis]==5.6.3
structlog==26.1.0
argon2-cffi==25.1.0  # password hashing (Argon2id)
httpx==0.28.1  # outgoing HTTP (Google sign-in)
email-validator==2.3.0  # EmailStr validation
